import React, { use } from "react";

const Background = () => {
  return (
    <div className="bg-bg-img-1 w-screen h-screen">
      <p className="text-black">Lort</p>
    </div>
  );
};

export default Background;
